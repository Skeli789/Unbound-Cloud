New Features:
-Ads
-GTS (and make sure bad words are replaced if necessary)

Feature Modifications:
-Timeout if friend trade connection has gone on too long
-Print loading while friend code is being fetched (after clicking create code)
-Ask if done using site after saving (but not if for a segue into trading)

Server Changes:
-Change the file signature for Unbound
